<% include partials/header.ejs%>
<h1> Simple Blog </h1>
<% blogs.forEach(function(blog) { %>
<div>
    <h2> <%=blog.title%></h2>
    <img src="<%= blog.image %>" width="300px">
    <span><%= blog.created %></span>
    <p> <%= blog.body %></p>
</div>
<% }) %>

<% include partials/footer.ejs%>


